# PHP Login Form with MySQL

This project demonstrates a simple login system using PHP and MySQL.

## Features
- Secure user login using PHP.
- Passwords are hashed with `password_hash`.
- Database connection using MySQL.

## Requirements
- PHP
- MySQL
